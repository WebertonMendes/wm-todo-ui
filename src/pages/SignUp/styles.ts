import styled from "styled-components";

export const Link = styled.div`
  font-size: 0.8rem;

  a {
    color: var(--blue-900);
    transition: 0.4s;
  }

  a:hover {
    color: var(--blue-300);
  }
`;
